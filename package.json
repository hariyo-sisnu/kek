{
  "name": "kek",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.client.json && cp dist/client/app.js public/app.js",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && node dist/server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@solana/web3.js": "^1.99.0",
    "dotenv": "^18.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3"
  }
}
